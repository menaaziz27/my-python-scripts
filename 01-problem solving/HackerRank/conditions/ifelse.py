# Is the Number Less than or Equal to Zero?
# Create a function that takes a number as 
# its only argument and returns True if it's
#  less than or equal to zero, otherwise return False.0

def check(num):
	return num <= 0

print(check(0))
print(check(5))